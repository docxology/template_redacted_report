# Redacted-report exemplar: the release-proof pipeline binds to the monorepo
# steganography verifier (watermark embed + Kmyth TPM sealing validation) in
# one adapter module. Everything else in src/ stays standalone.
allow_infrastructure_imports:
  - src/redacted_report/visuals.py
