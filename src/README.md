# src - template_redacted_report

Reusable redaction and release-audit code lives under `redacted_report/`.
`redacted_report/artifacts.py` owns typed fixture loading, strict public-safe
input validation, text-free public projections, and deterministic JSON writing.
